name: bump-version-number
on: [pull]
description: 'this will house the version number of the framework'
inputs:
  versionNumber:
  description: 'this will house the version number of the framework'
  required: true
runs:
  using: "composite"
  steps:
    - run: echo "I think I did it ${{ versionNumber }}"
